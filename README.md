# Android Termux Server

Lightweight private-cloud and application-hosting platform for Android/Termux.

This repository is being built for a Redmi Note 9. The control plane uses FastAPI + SQLite + vanilla browser JavaScript to keep RAM and CPU usage low and avoid Docker/Kubernetes.

See `docs/ARCHITECTURE.md` and `docs/TERMUX.md` for the design and deployment model.
